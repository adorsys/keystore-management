spring:
  datasource:
    url: jdbc:tc:mysql:8.0.17://127.0.0.1/keyrotation
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    initialization-mode: always
  main:
    allow-bean-definition-overriding: true
  flyway:
    enabled: true